#!/usr/bin/env bash

# Should be post-receive hook
# If you want to mirror - just call
# At the end hooks/post-receive.mirrorpush

lein exec -pe "(do \
  (require 'hivewing-core.hive-image) \
  (let [hive-uuid \"$GL_REPO\"] \
  (hivewing-core.hive-image/hive-images-set-update-message hive-uuid)))"
